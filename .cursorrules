# Cursor AI 全局行为准则 (Global Rules)

## 自动记录对话与进度
每次在对话中完成重要的技术探讨、方案设计、代码修改或任务阶段性进展后，**必须主动**将核心结论、进度总结或关键知识点，追加记录到项目根目录下的 `与AI协作全过程.md` 文件的末尾。

**执行要求：**
1. 不需要等待用户的额外指令，在回复用户的同时，直接调用文件编辑工具（如 `StrReplace` 或 `Write`）更新该文档。
2. 记录内容要求简明扼要、结构清晰（可使用 Markdown 列表或小标题）。
3. 确保更新操作在当前对话回合内完成。